#!/usr/bin/env sh
set -e
# https://docs.grommunio.com/admin/operations.html#mail-requeueing
QUEUE="${QUEUE:-/var/lib/gromox/queue}"
j=0
for saved in "$QUEUE"/save/*; do
	mess="$QUEUE/mess/$(date +%s)-$j"
	mv "$saved" "$mess"
	j=$((j+1))
	echo "requeued ${saved##*/} as ${mess##*/}"
	unset mess
done
